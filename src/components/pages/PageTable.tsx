import React from "react";
import Table from "../combo/table/Table";

const PageTable = (): JSX.Element => {
    return (
        <div id="PageTable" className="container-fluid">
            <Table />
        </div>
    );
};

export default PageTable;
